import React from 'react';
import styles from '../styles/Spinner.module.css'; // Import CSS module for styling

const Spinner = () => {
  return (
    <div className={styles.spinner}></div>
  );
};

export default Spinner;
